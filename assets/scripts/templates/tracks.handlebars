<div class="row">
  <div class="col-sm-8 track-main">
    {{#each tracks as |track|}}
      <div class="track">
        <h2 class="track-title">{{this.title}}</h2>
        <p class="track-meta">
          {{this.artist}}
        </p>
        <p>
          {{this.host_url}}
        </p>
      </div>
    {{/each}}
  </div>
</div>
